# Python packages for aura_health_profile scripts.
#
# PDF (md_to_pdf.py): pandoc is NOT a pip package — install the binary separately
#   (e.g. brew install pandoc). The script uses pandoc when it is on PATH; otherwise
#   mistune (AST) + ReportLab with a system CJK font or AURA_PDF_FONT.
#   For the most complex Markdown, pandoc remains the best option.

requests>=2.28.0
mistune>=3.0.0
reportlab>=4.0.0
pymupdf>=1.24.0
